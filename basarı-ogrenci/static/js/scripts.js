document.addEventListener("DOMContentLoaded", function() {
    // Sayfa yüklendiğinde bir animasyon başlat
    const container = document.querySelector('.container');
    container.classList.add('animate__animated', 'animate__fadeIn');
});
